# Product Manager | CLI

Простая консольная программа для работы со списком товаров!
Позволяет **добавлять, изменять, удалять товары** и **смотреть общую сумму**.


## Структура проекта

```
project/
├── src/
│   ├── data.txt       # Пример файла с товарами
│   ├── main.py        # ГЛАВНЫЙ ФАЙЛ
│   ├── manager.py     # Логика работы
│   ├── product.py     # Модель
│   └── storage.py     # Работа с файлом
├── gitignore          # А сюда не надо :)
└── README.md          # Инструкция
```
---

### 📂 Формат файла

Файл с товарами должен содержать строки в формате:

### Пример:

- Огурцы — 60.0
- Помидоры — 40.0
- Масло — 40.0
- Яблоки — 30.0


> Обратите внимание: между названием и ценой используется **длинное тире `—`**

---

### Как запускать

1. Убедитесь, что установлен Python 3.  
2. Откройте терминал и перейдите в папку с проектом.
3. Активируйте виртуальное окружение (если есть):

```bash
source venv/bin/activate  # Mac/Linux
venv\Scripts\activate     # Windows
```

4. Запустите команду:

**Пример:** `python main.py <файл: data.txt> add <имя> <цена>`

---

### Доступные действия

1. Добавить товар
```
python main.py data.txt add <имя> <цена>

Пример:
python main.py data.txt add Яблоки 300000
```

2. Изменить цену товара
```
python main.py data.txt update <имя> <новая_цена>

Пример:
python main.py data.txt update Огурцы 699999
```

3. Удалить товар
```
python main.py data.txt delete <имя>

Пример:
python main.py data.txt delete Масло
```

4. Посмотреть общую сумму
```
python main.py data.txt total

Пример вывода:
Общая сумма продукта: 130.0
```